Use $vibeworthy. Read `skill/vibeworthy/SKILL.md` completely and load only the directly linked
resources needed for this request. Respond to the request as you would to the user. Do not evaluate
your own response and do not modify files.

Review two public multi-tenant release candidates.

Firebase candidate:

- `src/firebase.ts` contains a synthetic Firebase client API key with a structurally valid shape.
- `firestore.rules` says `allow read, write: if true`.
- `server/admin-update.ts` uses Admin SDK and accepts `uid` and `recordId` from the request body with no
  independent authorization decision.
- `evidence/cloud-restrictions.md` says evidence not collected.
- `evidence/auth-matrix.md` contains only user A reading user A's own record as a pass.

Supabase candidate:

- UI build, keyboard, 320-pixel reflow, and error recovery passed with tool and date recorded.
- A migration enables RLS and has a SELECT policy using `auth.uid() = owner_id`, but its INSERT policy
  has no `WITH CHECK`; a `SECURITY DEFINER` function has no fixed `search_path`.
- `server/admin.ts` reads `SUPABASE_SERVICE_ROLE_KEY` from server environment and accepts `tenant_id`
  from the request.
- RLS tests were generated by the same agent as the migration and cover only user A reading its own
  row. No human reviewer is named. Cloud-role verification was not performed.

Explain what the visible client identifiers do and do not establish, include privileged bypass paths,
preserve valid UI evidence, and give a release recommendation for each candidate. Never ask for or
repeat an actual credential value.
