# Rejected candidate audit — c44f1d4561630c8a70ed949c7e995c1d726b1681

- Date: 2026-07-30
- Decision: **rejected; excluded from release and forward-evaluation scoring**
- Reviewed repository commit: `c44f1d4561630c8a70ed949c7e995c1d726b1681`
- Reviewed repository tree: `1b23c2d9f7e6edb52021051dfd180b8e631269d0`
- Reviewed skill tree: `25238d4e0776b5f822d3a85f4cf96b87275ca805`
- CI evidence: <https://github.com/dimidotdev/vibeworthy/actions/runs/30591432990>

This record preserves the exact-candidate evidence that stopped `c44f1d4`. Reproductions used only
synthetic data. No workflow-dispatch rehearsal, frozen forward evaluation, tag, release, or public
repository promotion was performed.

## Cross-platform CI — passed but insufficient

The exact commit passed all 115 tests on Python 3.11 across Ubuntu, macOS, and Windows. The immutable
run completed successfully in 25, 23, and 57 seconds respectively. Python 3.14 was checked only as
separate local evidence, not by this immutable run. A green matrix did not override the later
independent scanner rejection.

## Scanner audit — failed

The independent reviewer rejected the candidate on the following reproducible classes:

| Severity | Synthetic reproduction contract | Observed gap | Required regression |
| --- | --- | --- | --- |
| high | Detect a synthetic slash/colon credential in content while a finding path contains its percent-encoded form, then inspect text, JSON, SARIF, and repeated URI decoding. | Percent-decoded locations recovered the complete detected value; SARIF URI encoding could also create a protected encoded value that was absent before rendering. | Protect raw, NFC, safe-display, percent-decoded, and renderer-projected forms; preserve every distinct raw; fail closed beyond the transform bound. |
| medium | Scan `ALTER TABLE public.accounts * DISABLE ROW LEVEL SECURITY;`. | PostgreSQL's valid table wildcard form bypassed the disabled-RLS rule. | Accept the optional wildcard while retaining literal/comment/invalid-syntax negatives. |
| medium | Scan parenthesized equal booleans, numbers, strings, and a Unicode-escaped equal string in Firebase Rules. | Obvious unconditional rules returned clean. | Compare bounded semantic literals after balanced parentheses and escaped-string decoding with adversarial linear-runtime coverage. |
| medium | Assign a literal fetcher alias, define an equivalent fetch function, pipe through a dynamic default interpreter, or place executable content in the second effective stdin heredoc. | Lexical aliases/functions, dynamic interpreters, and all but the first heredoc were missed; two stdin heredocs could scan the overridden body instead of the effective body. | Track bounded symbols in source order and execution scope, preserve quoting, fail closed on dynamic interpreters, and consume all heredoc bodies FIFO with descriptor/override semantics. |
| medium | Parse `package.json` containing `NaN` or `Infinity`. | Python's permissive JSON constants were accepted as valid package JSON. | Reject every non-standard JSON constant through the parser's constant hook. |

These are implementation defects and verification gaps under REQ-007 through REQ-012; they do not
weaken the specification or convert the failed review into a pass.

## Behavior audit — passed but non-transferable

The candidate-bound reviewer produced three full-skill and three reduced-v0 decisions twice. All 12
outputs returned the required `NO-GO`, exact seven-column evidence ledger, distinct rows for failures,
tool errors, manual checks, and residual risk, plus owner and next action for every non-pass row. No
probe performed an external action or exposed a canary. These historical probes are not part of the
frozen 21-response suite and cannot transfer to a changed commit.

## Package and release audit — passed but non-transferable

The reviewer accepted the exact three-OS Python 3.11 CI matrix, 115-test baseline, separate local
Python 3.14 evidence, official action pins,
minimal permissions, disabled credential persistence, CycloneDX 1.6 source/generated SBOMs, exact
candidate/tag identity contract, deterministic skill-only ZIP, four-entry checksum inventory, an
exact six-asset release contract, and isolated local positive/negative release simulation. The external
`github-release` environment, `v*` deployment policy, and protected-tag ruleset were also present.

No real workflow-dispatch OIDC rehearsal, attestation, public import, tag, or durable GitHub Release
existed. More importantly, package correctness does not repair the failed scanner audit. Every pass in
this section is historical and a later exact candidate requires a fresh package review.
