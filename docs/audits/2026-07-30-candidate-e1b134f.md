# Rejected candidate audit — e1b134f3309fdd696ead02dcd256fedc089238f9

- Date: 2026-07-30
- Decision: **rejected; excluded from release and forward-evaluation scoring**
- Reviewed repository commit: `e1b134f3309fdd696ead02dcd256fedc089238f9`
- Reviewed skill tree: `ddb30c9e1f847b11fa9d8cda3af3a6ccd7b14129`
- CI evidence: <https://github.com/dimidotdev/vibeworthy/actions/runs/30588227283>

This record preserves the candidate-bound CI and independent audit results that stopped candidate
`e1b134f`. Reproduction cases used synthetic data. Temporary behavior-probe output is not a release
artifact and none of these checks count toward the final frozen forward suite.

## Cross-platform CI — failed

Ubuntu passed all 98 tests. macOS and Windows rejected the exact candidate:

| Platform | Observed failure | Required regression |
| --- | --- | --- |
| macOS | A synthetic Git reached through a worktree-originated PATH symlink executed and created its marker because lexical `/var` and resolved `/private/var` origins were compared inconsistently. | Reject controlled lexical and resolved origins across platform aliases; never create the marker. |
| Windows | A PATH junction originating in the worktree was not rejected. | Reject worktree-originated junctions before resolving or executing Git. |
| Windows | The SQL line-mapping fixture took 1.661 seconds and exceeded its 1.5-second budget. | Preserve bounded linear behavior on every supported runner. |

This failed run is immutable evidence for this commit and cannot be replaced by later local passes.

## Scanner audit — failed

The independent reviewer confirmed the committed 98-test baseline on Python 3.11 and 3.14, then
rejected the candidate on these additional classes:

| Severity | Reproduction contract | Observed gap | Required regression |
| --- | --- | --- | --- |
| high | Scan a nested repository target with a marker-writing Git in a repository sibling PATH directory or through a platform path alias. | The scanner executed repository-controlled Git and could still report a clean filesystem fallback. | Bound trust to every visible enclosing repository boundary, preserve lexical origin, and never execute the marker. |
| high | Invoke the documented scanner command with a target-controlled `PYTHONPATH`, `queue.py`, or `sitecustomize.py`. | Python executed project code before scanner startup and returned an unstructured traceback. | Require isolated Python mode in every launcher, example, and test; markers remain absent. |
| high | Correlate a long detected value and its shorter suffix with a filename. | Overlapping match intervals exposed most of the longer value in text, JSON, and SARIF paths. | Merge every overlapping span using its leftmost boundary and redact the complete value. |
| high | Detect an NFC value while a filename contains the canonically equivalent NFD representation. | Normalization recovered the complete value from report locations. | Canonically normalize values and locations before matching; no format exposes the normalized value. |
| medium | Hide a remote fetch through control words, process wrappers, BusyBox, redirections, functions, `case`, shell builtins, command/process substitutions, or an unknown launcher. | Executable remote-content flows returned clean. | Classify supported forms, link relevant substitutions, and emit an unparsed blocker when execution intent is ambiguous. |
| medium | Use equal string, decimal, or null Firebase literals. | Obvious unconditional Firestore/RTDB rules were missed. | Compare bounded literal forms and retain unequal-literal negatives. |
| medium | Scan dense Firebase OR conditions below the default file-size limit. | Repeated bounded-window searches took 56.80 seconds for an 880,021-byte fixture. | Carry statement context in one forward pass and keep adversarial runtime bounded. |
| medium | Feed 10,000 distinct detected values into path redaction. | The trie consumed about 182 MB while configured limits projected much higher memory use. | Enforce a conservative deterministic value/character budget before constructing the automaton. |
| low | Fill suppression metadata with U+034F combining grapheme joiners. | Visually empty owners, approvers, reasons, and controls were accepted. | Require meaningful letter/number content and compare normalized identities conservatively. |
| low | Put a harmless top-level `install` key before an effective dangerous `scripts.install`, including duplicate script keys. | Findings pointed to the unrelated first key. | Map the effective key inside the effective `scripts` object and use the last duplicate, matching JSON semantics. |

The documented non-atomic worktree boundary and quiescent isolated-checkout requirement were accepted
and were not counted as defects.

## Behavior audit — passed but non-transferable

The independent reviewer ran three full-skill and three reduced-v0 probes twice, producing 12
candidate-bound decisions. All preserved release identity, the exact seven-column ledger, separate
failure/tool-error/manual-check/residual-risk rows, an owner and next action for every non-pass item,
correct `NO-GO` behavior, and the authority boundary. No probe performed an external action or made an
absolute assurance claim.

This historical pass neither repairs the rejected candidate nor counts toward the final 21-response
suite. Any changed candidate requires fresh behavior probes.

## Package and release audit — failed

The package reviewer accepted the Agent Skill shape, MIT/provenance boundaries, CycloneDX 1.6 SBOM,
exact `R == C` identity, reproducible ZIP, non-circular 4+2 asset model, immutable official action
pins, isolated inline Python, dual attestation verification, tag-only promotion, and durable GitHub
Release design. The candidate still failed because:

- its cross-platform CI was red;
- CI checkout retained credentials while pull-request tests executed repository code;
- the `workflow_dispatch` example suggested `1.0.0-rc.1` although the source SBOM identified
  `1.0.0`; and
- the external `github-release` environment/tag policy did not yet exist, no repository ruleset or
  branch protection was configured, and no real tag, OIDC attestation, six-asset release, or public
  import evidence existed.

A history scan found only one explicitly synthetic generic-key fixture. External release controls must
be configured before the tag, and a later exact candidate must pass a fresh package audit.
